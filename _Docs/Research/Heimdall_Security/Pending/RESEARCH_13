What are the industry-standard scoring methodologies for aggregating findings from a multi-module SAST tool into an overall security score, and how do commercial tools (Veracode, Checkmarx, SonarQube, Snyk) implement security scoring that is meaningful to both security engineers and non-technical stakeholders?

Survey the following scoring approaches and their trade-offs: raw vulnerability count (why this is misleading), severity-weighted counts (CVSS-based weighting), risk-density metrics (findings per KLOC or per file), the Veracode Software Composition Analysis score model, SonarQube's Security Rating (A-E) and its mapping to vulnerability counts, and the Snyk Security Score for containers and code.

For each approach, provide: the formula or algorithm used (where publicly documented), what severity thresholds trigger rating changes, how the score handles duplicate findings from overlapping rules, and whether the score accounts for context (e.g., a critical finding in dead code vs in a hot API endpoint).

Then address: what the academic and industry literature recommends as the correct statistical foundation for a security score (is it a risk metric, a compliance metric, or a quality metric?), how scores should be normalised across codebases of different sizes, whether time-series trending of a score provides more value than its absolute value, and what the OWASP Testing Guide or NIST SSDF (SP 800-218) recommend about security measurement programs.

Also cover: how to handle the aggregation problem when sub-scanners (injection, secrets, headers, TLS, container) have fundamentally different false positive rates and different severity calibrations — what normalisation or weighting approaches are principled rather than arbitrary?

Include any 2022-2025 research on security metric validity and whether SAST scores correlate with actual vulnerability density in production systems.
