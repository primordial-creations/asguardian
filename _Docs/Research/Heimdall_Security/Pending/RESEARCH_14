What are the state-of-the-art static analysis techniques for detecting authentication vulnerabilities (CWE-287, CWE-306, CWE-307, CWE-798) in Python web applications, and how do tools distinguish between an authentication implementation that is present-but-weak versus one that is simply absent?

Survey authentication vulnerability categories and the static patterns associated with each: missing authentication on sensitive endpoints (absence of auth decorator/dependency), weak password hashing (MD5/SHA1/bcrypt with too few rounds, plain text comparison), JWT implementation flaws (algorithm=none acceptance, missing signature verification, weak secret keys, missing expiry validation), session management failures (non-random session IDs, missing SameSite/Secure/HttpOnly cookie flags, insufficient session invalidation on logout), and insecure password reset flows (predictable tokens, missing expiry).

For Django specifically: what are the AST patterns for detecting when a view is missing @login_required or the LoginRequiredMixin, when AUTH_PASSWORD_VALIDATORS is missing or incomplete in settings.py, when SESSION_COOKIE_SECURE or CSRF_COOKIE_SECURE are False, and when password comparison uses == instead of check_password()?

For JWT implementations in Python (PyJWT, python-jose, authlib): what AST patterns indicate that an application accepts the algorithm from the token header, that verify=False is passed to decode(), or that the key is a short or predictable string?

Cover what tools currently detect these patterns: Bandit's auth-related rules, any Semgrep community rules for Django authentication, SonarQube's Python auth checks. Provide published false positive and false negative rates where available.

Include: OWASP Top 10 2021 A07 (Authentication Failures) statistics, any 2023-2025 studies on authentication vulnerability prevalence in Django/Flask/FastAPI open-source projects.
