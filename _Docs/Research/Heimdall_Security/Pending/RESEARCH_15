What are the current industry benchmarks, detection methodologies, and open problems for detecting deserialization vulnerabilities (CWE-502) using static analysis in Python, Java, and PHP, and how do tools handle the fundamental challenge that exploitability depends on available gadget chains rather than the deserialization call itself?

Provide a detailed survey covering: the mechanics of Python deserialization vulnerabilities (pickle, marshal, yaml.load, jsonpickle, shelve), Java deserialization (ObjectInputStream, XStream, Kryo, Jackson default typing), and PHP deserialization (__wakeup(), __destruct() magic method chains), including how each differs in terms of static detectability.

For Python specifically: what are the AST patterns that distinguish safe from unsafe YAML usage (yaml.safe_load vs yaml.load without Loader argument vs yaml.load with Loader=yaml.FullLoader vs Loader=yaml.UnsafeLoader), how does pickle vulnerability detection work when pickle.loads is called on data from a network source versus a trusted local file, and what context signals allow a tool to reduce false positives on legitimate internal-only pickle usage?

Cover tool capabilities: does Bandit's B301-B302 series correctly identify all Python deserialization patterns, what are its known false negatives (e.g., shelve-based deserialization, indirect pickle calls through pandas.read_pickle), and how do Semgrep and CodeQL compare?

For the gadget chain problem: can static analysis identify whether a codebase contains classes that would serve as gadget chains for a deserialization attack, and is there any published research on static gadget chain discovery?

Include: CVE statistics for deserialization vulnerabilities from 2020-2025, any published OWASP or NIST guidance on deserialization controls, and what the ysoserial and marshal tools reveal about gadget chain availability in common Java library versions.
