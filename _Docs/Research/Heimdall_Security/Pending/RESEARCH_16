What are the published techniques, benchmarks, and real-world efficacy data for static detection of supply chain security vulnerabilities in Python dependency manifests (requirements.txt, pyproject.toml, setup.cfg, Pipfile.lock), including dependency confusion, typosquatting, and known-vulnerable transitive dependencies?

Provide a comprehensive survey covering: how tools like pip-audit, Safety CLI, OWASP Dependency-Check, Snyk Open Source, and Dependabot detect known-vulnerable packages by querying vulnerability databases (OSV, PyPA Advisory Database, NVD, GitHub Advisory Database), what the recall rates are for each database relative to the others (are there CVEs in NVD not in OSV or vice versa), and how quickly each database is updated after a new CVE is published (time-to-database latency).

For transitive dependency analysis: what are the algorithmic approaches for resolving the full dependency tree from a requirements.txt (which lists only direct dependencies) without executing pip install, what tools attempt this purely statically, and what accuracy do they achieve for diamond dependency and version range resolution?

For dependency confusion attacks: what naming patterns and package metadata signals allow a static scanner to flag packages that might be squatted internal package names, what PyPI's namespace protection mechanisms are and their limitations, and whether any static tool currently implements dependency confusion detection.

For typosquatting: what the published research says about the scale of typosquatting packages on PyPI (as of 2023-2025), what detection algorithms exist (edit distance, visual similarity, keyboard proximity), and what false positive rates result from these approaches.

Include: any published SLAs or dataset coverage statistics from OSV.dev, PyPA Advisory Database, and GitHub Advisory Database, and recommendations from the SLSA framework (Supply-chain Levels for Software Artifacts) and SSDF (NIST SP 800-218) for dependency security controls.
