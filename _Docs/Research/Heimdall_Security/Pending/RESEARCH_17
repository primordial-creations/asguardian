What are the industry standards, detection methodologies, and published benchmarks for TLS/mTLS configuration analysis using static analysis of infrastructure-as-code files (Nginx configs, HAProxy configs, Kubernetes Ingress manifests, AWS ALB listener configs, Terraform TLS resources)?

Survey the following and provide specific configuration patterns, CWE mappings, and how static tools detect them: TLS 1.0/1.1 enablement (CWE-326), weak cipher suites (RC4, DES, 3DES, export ciphers, NULL ciphers), missing HSTS with insufficient max-age, self-signed certificate usage in production configurations, missing certificate revocation checking (OCSP stapling), TLS session resumption misconfigurations, and mTLS configurations that accept client certificates without proper validation.

For Python-specific TLS: what are the AST patterns indicating insecure ssl.SSLContext usage (ssl.CERT_NONE, protocol=ssl.PROTOCOL_TLSv1, check_hostname=False), how does this map to the requests library (verify=False), and what does the research say about the prevalence of verify=False in Python codebases?

Cover tool capabilities: Checkov's TLS-related rules for Terraform and Kubernetes, tfsec/Trivy TLS detection, SSL Labs grading methodology and what configuration parameters drive A+ vs A vs B grades, and testssl.sh coverage for static-equivalent configuration analysis.

For mTLS specifically: what are the Kubernetes configuration patterns (Istio PeerAuthentication, service mesh mTLS policies, cert-manager issuers) that should be validated, and what static checks can distinguish permissive mTLS (PERMISSIVE mode) from strict enforcement?

Include: current NIST SP 800-52 Rev 2 and Mozilla TLS Configuration Generator recommendations as the authoritative baseline, BSI TR-02102 German federal TLS guidelines, and any 2023-2025 internet-wide measurement studies on TLS configuration quality.
