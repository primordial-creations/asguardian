Research datetime and timezone handling anti-patterns in software systems, focusing on the most common bugs found in production codebases across Python, Java, JavaScript, and Go. Document the taxonomy of datetime bugs: naive datetime usage (timezone-unaware objects), DST transition errors, epoch conversion mistakes, locale-dependent parsing without explicit format strings, mixing UTC and local time, storing datetimes as strings instead of timestamps, and calendar arithmetic errors (month boundaries, leap years). For each category, find empirical studies or large-scale code analysis that quantifies how frequently these patterns appear in open-source code. Look for research from the MSR (Mining Software Repositories) community on datetime-related bugs. Research the specific Python anti-patterns: using datetime.now() instead of datetime.utcnow() or datetime.now(timezone.utc), naive datetime arithmetic across DST boundaries, pytz versus dateutil versus zoneinfo for timezone handling, and the Python 3.9+ zoneinfo migration. For JavaScript, cover the notorious Date object issues, the Temporal proposal, and moment.js deprecation patterns. For Java, cover the pre-Java-8 Calendar pitfalls versus the java.time package. Document which of these anti-patterns are detectable by AST-level static analysis (examining constructor arguments and method call chains) versus those requiring dataflow analysis.
