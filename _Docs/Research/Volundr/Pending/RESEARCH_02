Research the NSA/CISA Kubernetes Hardening Guide (August 2021, updated 2022) and the CIS Kubernetes Benchmark (v1.8+) and produce a detailed mapping of their requirements to automated checks that tools like kube-bench, kubesec, Polaris, and Datree can verify programmatically on raw YAML manifests without cluster access.

I am building a Kubernetes manifest generator that produces YAML for Deployments, StatefulSets, DaemonSets, Jobs, CronJobs, Services, NetworkPolicies, PodDisruptionBudgets, ConfigMaps, and Secrets. I need to know: which hardening requirements can be validated purely by static analysis of the manifest YAML versus which require runtime inspection of the cluster? For the static-analysis-viable checks, what is the exact YAML path and expected value for each rule?

Cover the following specific control families: pod security contexts (runAsNonRoot, runAsUser, fsGroup, seccompProfile, appArmor annotations), container resource limits and requests, privilege escalation prevention (allowPrivilegeEscalation: false), capability dropping (drop ALL), read-only root filesystems, NetworkPolicy coverage (does every workload have an ingress and egress policy?), image pull policies and use of digest-pinned images rather than mutable tags, and service account token auto-mounting.

For each control, provide: the NSA/CISA or CIS benchmark reference ID, the YAML field path, the compliant value, and the risk of non-compliance. Also describe any known edge cases where the control cannot be applied (e.g., certain system-level DaemonSets legitimately need privileged mode).
