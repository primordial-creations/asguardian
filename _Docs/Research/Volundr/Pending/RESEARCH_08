Research Docker Compose file validation and Dockerfile best practices, with a focus on automated linting tools and what rules have the strongest empirical evidence for improving security and build reproducibility. I am building a Compose file generator and Dockerfile generator and need to know the state-of-the-art for output quality validation.

For Dockerfiles: cover Hadolint in depth — what rules does it enforce, which are classified as errors versus warnings, and which rules have documented security rationale? Specifically research: multi-stage build patterns and their impact on final image size and attack surface, the FROM pinning debate (digest SHA vs tag vs :latest), USER instruction placement and its interaction with package installation steps, COPY versus ADD semantics, layer caching optimization strategies and their interaction with security patching workflows, and the BuildKit secrets mount for avoiding secrets in layers.

For Docker Compose: what validation tools exist beyond basic YAML schema validation? Cover compose-spec conformance, research what Compose v2 (compose-spec) changed versus v3 versus v2.x, and document the fields that are most commonly misconfigured in production (healthcheck definitions, depends_on condition types, restart policies, volume mount permissions, network isolation between services). Are there security linters for Compose files analogous to Hadolint?

Research the tradeoffs between Compose for local development versus Kubernetes for production, and what patterns make a Compose file a good "specification document" that can be automatically translated to Kubernetes manifests. Cover Kompose and its known limitations.
