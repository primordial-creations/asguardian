Research the SLSA (Supply Chain Levels for Software Artifacts) framework versions 0.1 through 1.0 in depth, focusing on what each level requires of a CI/CD pipeline generator. I am building a system that generates pipeline configurations for GitHub Actions, GitLab CI, Azure DevOps, Jenkins, and CircleCI. I need to understand what constraints SLSA levels 1, 2, and 3 place on pipeline structure.

Cover: build provenance attestation (what metadata must be captured and in what format — SLSA provenance v1.0 predicate, in-toto attestation bundles), hermetic builds (network isolation, deterministic toolchains), two-party review requirements, and the distinction between hosted versus self-hosted runners in the context of SLSA trust levels. Which SLSA level can realistically be achieved on each of the five platforms I mentioned, and what are the blockers for reaching higher levels?

Research supply chain attacks that have targeted CI/CD systems: the SolarWinds build system compromise, the Codecov bash uploader attack, the 3CX supply chain attack, and GitHub Actions third-party action pinning vulnerabilities. What specific pipeline configuration patterns would have mitigated each attack?

Include coverage of: artifact signing with Sigstore/Cosign, SBOM attestation in CI pipelines, dependency pinning strategies for pipeline steps (SHA pinning of Actions versus branch/tag references), secret management in pipelines (OIDC-based credential federation versus long-lived secrets), and pipeline-as-code security review tooling. What does the state-of-the-art look like for automated SLSA compliance verification in a pipeline linting tool?
