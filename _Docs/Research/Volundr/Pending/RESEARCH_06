Research Helm chart best practices and the empirical evidence for which lint rules and validation checks matter most for production reliability and security. I am building a Helm chart generator and values generator and need to understand what the community consensus is on chart quality, backed by data where possible.

Cover the following areas: the Helm lint rule set and what each rule detects, the Datree Helm plugin and its policy library, the artifact hub score and what criteria it uses, and the CNCF chart testing framework (ct lint, ct install). Which checks have the highest signal-to-noise ratio? Are there studies or large-scale analyses of public Helm charts on Artifact Hub that document common mistakes?

Research best practices for: values.yaml schema validation using JSON Schema (values.schema.json), the NOTES.txt template for post-install guidance, default resource limits and security contexts in generated templates, chart dependency management (subchart pinning versus range constraints), and hook lifecycle management. What are the most common sources of Helm chart upgrade failures in production — version incompatibilities, CRD upgrade ordering, immutable field changes?

Cover Helm chart security specifically: how do you validate that a chart does not introduce privileged containers, host network access, or excessive RBAC permissions? Are there tools that perform security policy enforcement on rendered chart output (treating "helm template" output as static Kubernetes YAML and running Kyverno or OPA Gatekeeper policies against it)? What is the performance cost of this approach?
