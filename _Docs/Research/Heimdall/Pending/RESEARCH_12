What are the published benchmarks, detection methodologies, and state-of-the-art approaches for scanning Git repositories for accidentally committed secrets, and how do pre-commit, pre-receive, and post-commit scanning architectures compare in security effectiveness?

Cover the architectural layers of Git secret scanning: pre-commit hooks (client-side, bypassable with --no-verify), pre-receive hooks (server-side, enforced by the Git hosting platform), periodic repository scanning (scanning the full commit history including orphaned commits, stash refs, and packed refs), and real-time push protection (blocking pushes containing detected secrets before they are stored).

For each architecture, provide: the security guarantees it provides and its bypass attack surface, latency constraints that affect which detection algorithms are practical (entropy analysis vs regex vs ML), what happens to secrets already committed before the control was deployed, and how git rewrite operations (filter-branch, BFG Repo Cleaner, git-filter-repo) interact with secret remediation.

Cover the specific capabilities of: GitHub Advanced Security push protection (partner patterns vs custom patterns, the override mechanism and its audit log), GitLab Secret Detection (pre-receive, historical scanning), and the open-source tools GitLeaks and TruffleHog in server-side deployment mode.

Then address: what the industry considers the mean time to rotation (MTTR) for a leaked secret after detection, what studies exist on the real-world exploitation timeline for secrets discovered in public GitHub repositories (time-to-exploit after first commit), and what the research literature recommends as the minimum coverage for a Git secret scanning program in terms of secret types and commit history depth.

Include any 2023-2025 empirical studies on the prevalence of secrets in public repositories.
