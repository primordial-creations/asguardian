# Core dependencies (all built into Python 3.8+)
# No external dependencies required for basic scanning

# Optional dependencies for enhanced functionality
# requests>=2.28.0  # For HTTP-based scans (ssl_checker, http_security_headers)
# dnspython>=2.3.0  # For DNS security checks

# Development/Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
